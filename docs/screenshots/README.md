# Screenshots for the README

Drop PNGs here with these exact filenames — the main `README.md` already links
to them. Capture at a reasonable window size (the ~1536×837 pt default is fine);
retina is great. Crop to the window (⌘⇧4 then Space, click the window).

| filename | mode | what to show |
|---|---|---|
| `explore-venn.png` | **Explore** | A real query with **3–6 tags** selected so the Venn shows overlapping circles, a couple of **painted (blue) regions**, and the **region chips** row underneath. The money shot. |
| `grid-preview.png` | **Tags** | A boolean query active — a few **include/exclude chips** in the query bar — with the **results grid populated** and the **split preview open** on the right showing a file + its info/EXIF. |
| `queue.png` | **Queue** | The queue with a watched folder: a mix of **files and a folder**, the **Type** filter visible, ideally **drilled one level** so the breadcrumb (🏠 › Folder) shows. |
| `tag-map.png` | **Explore** | The circle-pack **tag map** (the colored bubbles) with no query — the browse view. |

Optional: a wide **`hero.png`** (any striking view, ~1600px wide) for the top of
the README — if you add one, uncomment the hero line in `README.md`.
